# ===================================
#  Form Field Definitions
# ===================================

fields:
    name:
        label: 'dimsog.blocks::lang.models.general.name'
        required: true