# ===================================
#  List Column Definitions
# ===================================

columns:
    id:
        label: 'dimsog.blocks::lang.models.general.id'
        searchable: true
        width: '100px'
    name:
        label: 'dimsog.blocks::lang.models.general.name'
        searchable: true
